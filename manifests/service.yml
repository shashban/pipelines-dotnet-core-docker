apiVersion: v1
kind: Service
metadata:
    name: shashbanpipelinesdotnetcoredocker
spec:
    type: LoadBalancer
    ports:
    - port: 3000 
    selector:
        app: shashbanpipelinesdotnetcoredocker